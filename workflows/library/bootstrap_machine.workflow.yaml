api_version: v1
kind: Workflow
metadata:
  name: bootstrap_machine
  version: 0.1.0
  tags: [bootstrap]
spec:
  steps:
    - id: bootstrap
      action: shell
      with:
        cmd: "./scripts/bootstrap.sh"
